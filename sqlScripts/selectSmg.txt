Получение всех статусов:
SELECT status FROM statuses;

Получение данных конкретного пользователя (использовала для получения id):
async getUser(channel_id) {
    await users.createTable();
    return sql`SELECT * FROM users
    WHERE channel_id = ${channel_id};`.then(res => { return _.first(res); });;
},

